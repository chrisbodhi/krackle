{
  "name": "krackle",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "bun build ./krackle.ts --outfile krackle.esm.min.js --format esm --target browser --minify",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@types/bun": "1.3.14"
  },
  "peerDependencies": {
    "typescript": "5.9.3"
  }
}
